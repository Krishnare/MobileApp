import { StyleSheet } from "react-native";
export default (styles = StyleSheet.create({
  logo: {
    height: 60,
    width: 60
  },
  headerText: {
    fontSize: 60,
    fontWeight: "bold",
    marginTop: 20
  }
}));
